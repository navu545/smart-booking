{
  "name": "smart-booking",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build"
  },
  "devDependencies": {
    "turbo": "^2.9.6"
  },
  "packageManager": "npm@11.0.0",
  "engines": {
    "node": ">=18",
    "npm": ">=11"
  }
}
